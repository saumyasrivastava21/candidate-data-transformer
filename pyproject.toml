[project]
name = "candidate-data-transformer"
version = "0.1.0"
description = "Multi-source candidate data transformer for canonical profile generation"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]